<form method="post" action="{{ path('app_j_o_article_delete', {'id': j_o_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ j_o_article.id) }}">
    <button class="btn btn-danger" style="margin-left: 30px !important;">Supprimer</button>
</form>
